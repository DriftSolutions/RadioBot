SET(THIS_EXE add_checksum)

AUX_SOURCE_DIRECTORY(. SRCFILES)
#AUX_SOURCE_DIRECTORY(../../Common/libzip LZIPFILES)
add_executable (${THIS_EXE} ${SRCFILES}) 
TARGET_LINK_LIBRARIES(${THIS_EXE} ${LIBS})

