SET_TARGET_PROPERTIES(${THISPLUG} PROPERTIES PREFIX "")
TARGET_LINK_LIBRARIES(${THISPLUG} ${PLUGLIBS} ${LIBS})

add_custom_command(
        TARGET ${THISPLUG}
        POST_BUILD
        COMMAND ${EXECUTABLE_OUTPUT_PATH}/add_checksum ${LIBRARY_OUTPUT_PATH}/${THISPLUG}.so
)

SET(PLUGLIBS "")
