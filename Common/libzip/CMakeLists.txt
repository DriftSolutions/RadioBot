
AUX_SOURCE_DIRECTORY(. SRCFILES)
add_library (zip-static STATIC ${SRCFILES}) 
#SET_TARGET_PROPERTIES(zip-static PROPERTIES LIBRARY_OUTPUT_DIRECTORY .)
SET_TARGET_PROPERTIES(zip-static PROPERTIES ARCHIVE_OUTPUT_DIRECTORY ../../obj)
