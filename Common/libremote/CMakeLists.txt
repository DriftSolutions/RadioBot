
AUX_SOURCE_DIRECTORY(. SRCFILES)
add_library (remote STATIC ${SRCFILES}) 
#SET_TARGET_PROPERTIES(remote PROPERTIES LIBRARY_OUTPUT_DIRECTORY .)
SET_TARGET_PROPERTIES(remote PROPERTIES ARCHIVE_OUTPUT_DIRECTORY ../../obj)
