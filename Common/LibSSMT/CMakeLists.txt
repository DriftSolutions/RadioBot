
AUX_SOURCE_DIRECTORY(. SRCFILES)
add_library (ssmt STATIC ${SRCFILES}) 
#SET_TARGET_PROPERTIES(ssmt PROPERTIES LIBRARY_OUTPUT_DIRECTORY .)
SET_TARGET_PROPERTIES(ssmt PROPERTIES ARCHIVE_OUTPUT_DIRECTORY ../../obj)
